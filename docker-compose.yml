networks:
  proxy_sql-network:
    external: true
services:
  pinball-service:
    image: nginx:alpine
    container_name: pinball
    hostname: pinball
    networks:
      - proxy_sql-network
    volumes:
      - /src/pinball/web:/usr/share/nginx/html:ro
    restart: unless-stopped
